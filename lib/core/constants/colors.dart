import 'package:flutter/material.dart';

class AppColors {
  static const primary = Color(0xFF20232A); // RAWG dark background
  static const accent = Color(0xFF61DAFB);  // RAWG accent blue
  static const textPrimary = Colors.white;  // Primary text color
  static const textSecondary = Color(0xFF8D99AE); // Secondary text color
  static const cardBackground = Color(0xFF2E3440); // Card background
  static const buttonBackground = Color(0xFF61DAFB);
}
